#pragma once
#include "detail.hpp"

// Determined version

namespace myalgo
{
    template<typename T>
    T kth_element(T a[], const size_t& k, const size_t& left, const size_t& right) {
        if ((right - left) == 0) {
            return a[0];
        }



    }
}